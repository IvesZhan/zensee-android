<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/barLabelText"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textColor="@color/zs_text_subtle"
        android:textSize="13sp" />

    <ProgressBar
        android:id="@+id/barProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="0dp"
        android:layout_height="12dp"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:max="60"
        android:progressTint="@color/zs_primary"
        android:progressBackgroundTint="@color/zs_surface_alt" />

    <TextView
        android:id="@+id/barValueText"
        android:layout_width="48dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:gravity="end"
        android:textColor="@color/zs_text_main"
        android:textSize="13sp" />
</LinearLayout>

# ZenSee Android Claude Entry

Read `AGENTS.md` at the repository root and treat it as the canonical project rules file.

Do not maintain a separate duplicated rules body here unless the user explicitly asks for it.
